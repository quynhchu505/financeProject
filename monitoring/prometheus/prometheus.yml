global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: finance_backend
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:8000']
